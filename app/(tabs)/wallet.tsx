import { StyleSheet } from "react-native";
import ScreenWrapper from "@/components/ScreenWrapper";
import Typo from "@/components/Typo";

const WalletScreen = () => {
  return (
    <ScreenWrapper>
      <Typo>WalletScreen</Typo>
    </ScreenWrapper>
  );
};

export default WalletScreen;

const styles = StyleSheet.create({});
